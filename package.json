{
  "name": "career-disha",
  "version": "1.0.0",
  "private": true,
  "description": "CareerDisha — AI career guidance for Indian students. Static HTML + serverless Groq proxy (Netlify Functions / Vercel Functions) with response caching.",
  "scripts": {
    "refresh-data": "node scripts/refresh-market-data.js",
    "refresh-data:dry": "node scripts/refresh-market-data.js --dry-run",
    "generate-data": "node scripts/generate-data-js.js",
    "test:data": "node data/_test-matcher.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@vercel/kv": "^3.0.0"
  }
}
